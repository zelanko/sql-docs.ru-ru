---
title: Задание свойств подключения | Документы Майкрософт
ms.custom: ''
ms.date: 07/31/2019
ms.prod: sql
ms.prod_service: connectivity
ms.reviewer: ''
ms.technology: connectivity
ms.topic: conceptual
ms.assetid: f1b62700-f046-488d-bd6b-a5cd8fc345b7
author: MightyPen
ms.author: genemi
ms.openlocfilehash: 7a48cfc1b860ead763facd839830429d478e415f
ms.sourcegitcommit: a1adc6906ccc0a57d187e1ce35ab7a7a951ebff8
ms.translationtype: MTE75
ms.contentlocale: ru-RU
ms.lasthandoff: 08/09/2019
ms.locfileid: "68891362"
---
# <a name="setting-the-connection-properties"></a>Задание свойств соединения

[!INCLUDE[Driver_JDBC_Download](../../includes/driver_jdbc_download.md)]

Свойства строки подключения могут быть заданы различными способами.

- В виде свойств имя=значение в URL-адресе подключения при подключении с помощью класса DriverManager.
- В качестве свойств Name = Value в параметре *Properties* метода Connect в классе диспетчер драйверов.
- С помощью аргумента соответствующего метода задания свойств источника данных драйвера. Пример:  
  
    ```java
    datasource.setServerName(value)  
    datasource.setDatabaseName(value)  
    ```  
  
## <a name="remarks"></a>Remarks

В именах свойств не учитывается регистр символов. Дублирующиеся имена свойств разрешаются в следующем порядке:  
  
1. аргументы API (например, имя пользователя и пароль);
2. Коллекция свойств.  
3. Последний экземпляр в строке подключения.
  
Кроме того, в именах свойств разрешено использовать неизвестные значения, и драйвер JDBC не проверяет их значения на предмет учета регистра.

Допускается использование синонимов, которые разрешаются в порядке очередности, аналогично повторяющимся именам свойств.

В следующей таблице перечислены все доступные на данный момент свойства строки подключения для драйвера JDBC.

| Свойство<br/>Тип<br/>По умолчанию | Описание |
| :------------------------------ | :---------- |
| accessToken<br/><br/>String<br/><br/>null | Это свойство используется для подключения к базе данных SQL с помощью маркера доступа. **accessToken** нельзя задать с помощью URL-адреса соединения. |
| applicationIntent<br/><br/>String<br/><br/>ReadWrite | Объявляет тип рабочей нагрузки приложения при соединении с сервером. <br/><br/>Возможные значения — **ReadOnly** и **ReadWrite**. <br/><br/>См. подробнее о [поддержке драйвера JDBC для обеспечении высокой доступности и аварийного восстановления](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md). |
| applicationName<br/><br/>String<br/>[&lt;=128 char]<br/><br/>null | Имя приложения или "[!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)]", если имя не указано.<br/><br/>Используется для идентификации определенных приложений в различных средствах профилирования и ведения журнала [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]. |
| проверка подлинности<br/><br/>String<br/><br/>NotSpecified | Начиная с Microsoft JDBC Driver 6,0 для SQL Server это необязательное свойство указывает метод проверки подлинности SQL, используемый для соединения. Возможные значения: **ActiveDirectoryIntegrated**, **ActiveDirectoryPassword**, **активедиректоримси**, **SqlPassword**и **NotSpecified**по умолчанию.<br/><br/> Используйте **ActiveDirectoryIntegrated** для подключения к базе данных SQL с помощью встроенной проверки подлинности Windows.<br/><br/> Используйте **ActiveDirectoryPassword** для подключения к базе данных SQL, используя имя и пароль участника Azure AD.<br/><br/> Используйте **активедиректоримси** для подключения к базе данных SQL из ресурса Azure, например виртуальной машины Azure, службы приложений или приложение-функция с использованием проверки подлинности управляемое удостоверение службы (MSI). <br><br>При использовании режима проверки подлинности **активедиректоримси** поддерживаются два типа управляемых удостоверений: <br> 1. _Управляемое удостоверение, назначенное системой_ : используется для получения **accessToken** по умолчанию. <br> 2. _Управляемое пользователем удостоверение, назначаемое пользователю_ : используется для получения **ACCESSTOKEN** , если идентификатор клиента управляемое удостоверение службы (MSI) указан с помощью свойства соединения **мсиклиентид** .<br/><br/> Используйте **SqlPassword** для подключения к SQL Server с помощью свойств **username**/**и** **Password**.<br/><br/> Используйте **NotSpecified** , если ни один из этих методов проверки подлинности не требуется.<br/><br/> **Важно.**  Если для проверки подлинности задано значение ActiveDirectoryIntegrated, необходимо установить следующие две библиотеки: **SQLJDBC_AUTH. DLL** (доступно в пакете драйверов JDBC) и Azure Active Directory Библиотека проверки подлинности для SQL Server (**ADALSQL. DLL**) он доступен на разных языках (для x86 и AMD64) из центра загрузки [Майкрософт Библиотека проверки подлинности Active Directory для Microsoft SQL Server](https://www.microsoft.com/download/details.aspx?id=48742). Драйвер JDBC поддерживает только версии **1.0.2028.318 и выше** для ADALSQL. Компоновки.<br/><br/> **Примечание.**  Если для свойства Authentication задано любое значение, отличное от **NotSpecified**, драйвер по умолчанию использует шифрование по протоколу SSL (SSL).<br/><br/> Сведения о настройке проверки подлинности Azure Active Directory см. в подсоединении [к базе данных SQL с помощью проверки Подлинности Azure Active Directory](https://azure.microsoft.com/documentation/articles/sql-database-aad-authentication/). |
| authenticationScheme<br/><br/>String<br/><br/>NativeAuthentication | Указывает, какой тип встроенной безопасности должен использоваться приложением. Возможные значения: **жавакерберос**, **NTLM**и **нативеаусентикатион**по умолчанию.<br/><br/> При использовании **аусентикатионсчеме = жавакерберос**необходимо указать полное доменное имя (FQDN) в свойстве **ServerName** или **serverSpn** . В противном случае возникает ошибка (сервер не найден в базе данных Kerberos).<br/><br/> См. об использовании **authenticationScheme=JavaKerberos** в руководстве по [встроенной аутентификации Kerberos для подключения к SQL Server](../../connect/jdbc/using-kerberos-integrated-authentication-to-connect-to-sql-server.md). <br/><br/> При использовании **аусентикатионсчеме = NTLM**необходимо указать домен Windows для проверки подлинности в с помощью NTLM в свойстве **domain** или **domainName** , а также учетные данные Windows в поле **пользователь** или **имя** пользователя и **пароль.** свойства. В противном случае возникает ошибка (необходимо указать свойства соединения).  |
| cancelQueryTimeout<br/><br/>INT<br/><br/>-1 | Начиная с Microsoft JDBC Driver 6,4 для SQL Server это свойство можно использовать для отмены установленного в соединении параметра **queryTimeout** . Выполнение запроса зависает и не создает исключение, если TCP-соединение с SQL Server автоматически удаляется. Это свойство применимо, только если для соединения также задано значение "queryTimeout". <br/><br/>Драйвер ожидает общий объем **канцелкуеритимеаут** + **queryTimeout** секунд, чтобы удалить подключение и закрыть канал. <br/><br/>Значение этого свойства по умолчанию равно-1, а поведение — неограниченное время ожидания. |
| columnEncryptionSetting<br/><br/>String<br/>["Enabled" &#124; "Disabled"]<br/><br/>Выключено | Переключите на "Включено" для использования функции Always Encrypted (AE), начиная с Microsoft JDBC Driver 6.0 для SQL Server. Когда функция Always Encrypted включена, драйвер JDBC прозрачно шифрует и расшифровывает конфиденциальные данные, хранящиеся в столбцах зашифрованной базы данных в SQL Server.<br/><br/> Дополнительные сведения о **columnEncryptionSetting**см. в статье [Использование Always encrypted с драйвером JDBC](../../connect/jdbc/using-always-encrypted-with-the-jdbc-driver.md) .<br/><br/> **Примечание.**  Always Encrypted доступна в SQL Server 2016 или более поздних версиях. |
| databaseName,<br/>База данных<br/><br/>String<br/>[&lt;=128 char]<br/><br/>null | Имя базы данных для соединения. <br/><br/>Если не установлено, подключение выполняется к базе данных по умолчанию. |
| domainName,<br/>домен<br/><br/>String<br/>null | Домен Windows для проверки подлинности в с использованием NTLM. |
| disableStatementPooling<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>true | Флаг, указывающий, следует ли использовать пулы инструкций. |
| Енаблепрепареонфирст...<br/>препаредстатементкалл<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>false | _енаблепрепареонфирстпрепаредстатементкалл_<br/><br/> Задайте значение "true", чтобы включить создание подготовленного обработчика инструкции <code>sp_prepexec</code> путем вызова метода при первом выполнении подготовленной инструкции. <br/><br/>Задайте значение "false", чтобы изменить первое выполнение подготовленной инструкции для вызова <code>sp_executesql</code> и не подготавливать инструкцию, как только второе выполнение вызовет <code>sp_prepexec</code> настройку подготовленного обработчика инструкции. |
| encrypt<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>false | Установите значение true, чтобы указать, что [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] использует шифрование SSL для всех данных, передаваемых между клиентом и сервером, при условии что на сервере установлен сертификат. Значение по умолчанию — «false».<br/><br/> Начиная с Microsoft JDBC Driver 6,0 для SQL Server существует новый параметр подключения Authentication, который по умолчанию использует SSL-шифрование. <br/><br/>Дополнительные сведения см. в разделе, описывающем свойство authentication. |
| failoverPartner<br/><br/>String<br/><br/>null | Имя сервера отработки отказа, используемого в конфигурации зеркального отображения базы данных. Это свойство используется при ошибке первоначального подключения к основному серверу. После выполнения первоначального подключения это свойство игнорируется. Должно использоваться совместно со свойством databaseName.<br/><br/> **Примечание**. Драйвер не поддерживает указание номера порта экземпляра сервера для экземпляра партнера по обеспечению отработки отказа в составе свойства failoverPartner в строке подключения. Однако поддерживается указание свойств serverName, instanceName и portNumber экземпляра основного сервера и свойства failoverPartner экземпляра партнера по обеспечению отработки отказа в одной строке подключения.<br/><br/> Если в свойстве подключения **Server** указано имя виртуальной сети, то использовать зеркальное отображение базы данных нельзя. См. подробнее о [поддержке драйвера JDBC для обеспечении высокой доступности и аварийного восстановления](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md). |
| fips<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>"false" | Для FIPS Enabled ВИРТУАЛЬНОЙ машины Java это свойство должно иметь **значение true**. |
| fipsProvider<br/><br/>String<br/><br/>null | Поставщик FIPS, настроенный в ВИРТУАЛЬНОЙ машины Java. Например, БКФИПС или SunPKCS11-NSS. Удалено в версии 6.4.0. подробности см. [здесь](https://github.com/Microsoft/mssql-jdbc/pull/460). |
| гсскредентиал<br/><br/>org. IETF. жгсс. Гсскредентиал<br/><br/>null | Начиная с Microsoft JDBC Driver 6,2 для SQL Server учетные данные пользователя, используемые для ограниченного делегирования Kerberos, могут передаваться в этом свойстве. <br/><br/>Он должен использоваться с **IntegratedSecurity** как **true** и **жавакерберос** как **аусентикатионсчеме**. |
| hostNameInCertificate<br/><br/>String<br/><br/>null | Имя узла используется для проверки SSL-сертификата [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)].<br/><br/> Если свойство hostNameInCertificate не указано или равно null, то [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] использует значение свойства **serverName** в URL-адресе подключения в качестве имени узла для проверки SSL-сертификата [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)].<br/><br/> **Примечание.**  Это свойство используется в сочетании со свойствами **шифрования**/для**проверки** подлинности и свойством **trustServerCertificate** . Это свойство влияет на проверку сертификата, только если соединение использует шифрование SSL (SSL), а **trustServerCertificate** имеет значение false. Для успешного установления SSL-подключения передаваемое в **hostNameInCertificate** значение должно совпадать с общим именем (CN) или DNS-именем в поле "Альтернативное имя субъекта" сертификата сервера. См. [основные сведения о поддержке SSL](../../connect/jdbc/understanding-ssl-support.md). |
| INSTANCENAME<br/><br/>String<br/>[&lt;=128 char]<br/><br/>null | Имя экземпляра [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)], к которому производится подключение. Если не задано, подключение выполняется к экземпляру по умолчанию. В случае, когда указан порт и instanceName, см. примечания к порту.<br/><br/> Если в свойстве подключения **Server** указано имя виртуальной сети, то использовать свойство подключения **instanceName** нельзя. См. подробнее о [поддержке высокого уровня доступности и аварийного восстановления в драйвере JDBC](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md). |
| integratedSecurity<br/><br/>boolean<br/>["true"&#124;"false"]<br/><br/>false | Задайте значение "true", чтобы указать, что учетные данные [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] Windows используются в операционных системах Windows. Если значение равно true, драйвер JDBC выполняет поиск учетных данных в кэше учетных данных локального компьютера, который был предоставлен при входе пользователя на компьютер или в сеть.<br/><br/> Задайте значение "true" (WITH **аусентикатионсчеме = жавакерберос**), чтобы указать, что учетные данные Kerberos [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]используются. Дополнительные сведения см. в статье [Использование встроенной проверки подлинности Kerberos для подключения к SQL Server](../../connect/jdbc/using-kerberos-integrated-authentication-to-connect-to-sql-server.md). <br/><br/> Задайте значение "true" (WITH **аусентикатионсчеме = NTLM**), чтобы указать, что учетные данные NTLM [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]используются. <br/><br/> Если установлено значение false, необходимо предоставить имя пользователя и пароль. |
| жаасконфигуратионнаме<br/><br/>String<br/><br/>SQLJDBCDriver | Начиная с драйвера Microsoft JDBC Driver 6,2 для SQL Server каждое подключение к SQL Server может иметь собственный файл конфигурации входа JAAS для установления подключения Kerberos. Имя файла конфигурации входа может быть передано через это свойство. <br/> По умолчанию свойство `useDefaultCcache = true` набора драйверов для IBM виртуальных машин Java и `useTicketCache = true` для других виртуальных машин Java. |
| keyStoreAuthentication<br/><br/>String<br/><br/>null | Начиная с Microsoft JDBC Driver 6,0 для SQL Server это свойство определяет, какое хранилище ключей легко настроить для подключения к Always Encrypted и определяет механизм проверки подлинности, используемый для проверки подлинности в хранилище ключей. Драйвер Microsoft JDBC Driver 6,0 для SQL Server поддерживает настройку хранилища ключей Java без проблем с помощью этого свойства, для которого необходимо задать значение "**keyStoreAuthentication = JavaKeyStorePassword**". Обратите внимание, что для использования этого свойства также необходимо задать свойства **keyStoreLocation** и **KeyStoreSecret** для хранилища ключей Java. <br/><br/>Дополнительные сведения: [Использование функции Always Encrypted с драйвером JDBC](https://msdn.microsoft.com/library/mt591987%28v=sql.110%29.aspx?f=255&MSPPError=-2147217396). |
| keyStoreLocation<br/><br/>String<br/><br/>null | Если **keyStoreAuthentication = JavaKeyStorePassword**, свойство **keyStoreLocation** определяет путь к файлу хранилища ключей Java, в котором хранится главный ключ столбца для использования с данными Always encrypted. Обратите внимание, что путь должен включать имя файла хранилища ключей.<br/><br/>Дополнительные сведения: [Использование функции Always Encrypted с драйвером JDBC](https://msdn.microsoft.com/library/mt591987%28v=sql.110%29.aspx?f=255&MSPPError=-2147217396). |
| keyStoreSecret<br/><br/>String<br/><br/>null | Если **keyStoreAuthentication = JavaKeyStorePassword**, свойство **keyStoreSecret** определяет пароль, который будет использоваться для хранилища ключей, а также для ключа. Обратите внимание, что для использования хранилища ключей Java необходимо, чтобы ключ ключей и пароль ключа совпадали.<br/><br/>Дополнительные сведения: [Использование функции Always Encrypted с драйвером JDBC](https://msdn.microsoft.com/library/mt591987%28v=sql.110%29.aspx?f=255&MSPPError=-2147217396). |
| lastUpdateCount<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>true | Если установлено значение true, возвращается только последнее значение счетчика обновлений из инструкции SQL, переданной серверу. Оно может быть использовано в одной из инструкций SELECT, INSERT, DELETE, чтобы игнорировать последующие изменения счетчика обновлений, вызываемые триггерами сервера. Установка для этого свойства значения false влечет за собой возврат всех значений счетчика обновлений, включая изменения, вызванные триггерами сервера.<br/><br/> **Примечание**. Это свойство применяется только при использовании с методами [executeUpdate](../../connect/jdbc/reference/executeupdate-method-sqlserverstatement.md). Все другие методы возвращают все результаты и значения счетчика обновлений. Это свойство влияет только на значения счетчика обновлений, возвращаемые триггерами сервера. Оно не влияет на результирующие наборы или ошибки, возникшие в процессе выполнения триггеров. |
| lockTimeout<br/><br/>INT<br/><br/>-1 | Время в миллисекундах, по истечении которого база данных сообщит об истечении времени ожидания блокировки. Режим по умолчанию — неограниченное время ожидания. Если задано, это значение по умолчанию для всех инструкций в подключении. Обратите внимание, что **инструкция. getquerytimeout ()** может использоваться для задания времени ожидания для конкретных инструкций. Значение может быть равным 0 — время ожидания отсутствует. |
| loginTimeout<br/><br/>INT<br/>[0..65535]<br/><br/>15 | Время в секундах, по истечении которого драйвер инициирует превышение времени ожидания неудачного подключения. Нулевое значение указывает, что время ожидания равно системному времени ожидания по умолчанию, которое по умолчанию составляет 15 секунд. Ненулевое значение представляет время в секундах, по истечении которого драйвер инициирует превышение времени ожидания неудачного подключения.<br/><br/> Если в свойстве подключения **Server** указано имя виртуальной сети, то необходимо задать значение ожидания три минуты или более, чтобы была возможна отработка отказа при подключении. См. подробнее о [поддержке высокого уровня доступности и аварийного восстановления в драйвере JDBC](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md). |
| мсиклиентид<br/><br/>String<br/><br/>null | Идентификатор клиента Управляемое удостоверение службы (MSI), который будет использоваться для получения **accessToken** при установлении соединения с режимом проверки подлинности **активедиректоримси** .|
| multiSubnetFailover<br/><br/>Логическое значение<br/><br/>false | Всегда указывайте **MultiSubnetFailover=True** при соединении с прослушивателем группы доступности [!INCLUDE[ssSQL11](../../includes/sssql11-md.md)] или экземпляром отказоустойчивого кластера [!INCLUDE[ssSQL11](../../includes/sssql11-md.md)]. **multiSubnetFailover=true** настраивает [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] для ускоренного обнаружения активного (в данный момент) сервера и подключения к нему. Допустимые значения — true и false. См. подробнее о [поддержке высокого уровня доступности и аварийного восстановления в драйвере JDBC](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md).<br/><br/> Доступ к свойству подключения **multiSubnetFailover** программным способом возможен через свойства [getPropertyInfo](../../connect/jdbc/reference/getpropertyinfo-method-sqlserverdriver.md), [getMultiSubnetFailover](../../connect/jdbc/reference/getmultisubnetfailover-method-sqlserverdatasource.md) и [setMultiSubnetFailover](../../connect/jdbc/reference/setmultisubnetfailover-method-sqlserverdatasource.md).<br/><br/> **Примечание.**  Начиная с Microsoft JDBC Driver 6,0 для SQL Server больше не требуется задавать значение "true" для параметра **MultiSubnetFailover** при подключении к прослушивателю группы доступности. Новое свойство, **transparentNetworkIPResolution**, включенное по умолчанию, обеспечивает обнаружение и подключение к активному серверу (в настоящее время). |
| packetSize<br/><br/>INT<br/>[-1 &#124; 0 &#124; 512..32767]<br/><br/>8000 | Размер (в байтах) сетевого пакета, используемого для обмена данными с SQL Server. Значение -1 указывает на использование размера сетевого пакета сервера по умолчанию. Значение 0 указывает на использование максимального значения — 32767. Если этому свойству присвоено значение за пределами допустимого диапазона значений, возникнет исключение.<br/><br/> **Важно**. Не рекомендуется использовать свойство packetSize при включенном шифровании (encrypt=true). В противном случае в работе драйвера может быть вызвана ошибка подключения. Дополнительные сведения см. в описании метода [setPacketSize](../../connect/jdbc/reference/setpacketsize-method-sqlserverdatasource.md) класса [SQLServerDataSource](../../connect/jdbc/reference/sqlserverdatasource-class.md). |
| password<br/><br/>String<br/>[&lt;=128 char]<br/><br/>null | Пароль базы данных в случае подключения к пользователю и паролю SQL.<br/>Для подключения Kerberos с именем участника и паролем этому свойству присваивается значение пароль субъекта Kerberos. |
| portNumber,<br/>port<br/><br/>INT<br/>[0..65535]<br/><br/>1433 | Порт, который прослушивает [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]. Если номер порта задан в строке подключения, запросы к SQLbrowser не выполняются. В случае, когда указан порт и instanceName, подключение выполняется к указанному порту. Однако выполняется проверка **instanceName**, и если значение этого параметра не соответствует порту, возникает ошибка.<br/><br/> **Важно**. Рекомендуется всегда указывать номер порта, так как это более безопасно, чем использование SQLbrowser. |
| queryTimeout<br/><br/>INT<br/><br/>-1 | Количество секунд ожидания до истечения времени ожидания в запросе. Значение по умолчанию —-1, что означает бесконечное время ожидания. Если задать для этого параметра значение 0, время ожидания не ограничено. |
| responseBuffering<br/><br/>String<br/>["Full" &#124; "адаптивно"]<br/><br/>adaptive | Если свойство имеет значение adaptive, это указывает на буферизацию необходимого минимума данных. По умолчанию используется режим adaptive.<br/><br/> Если свойство имеет значение full, при выполнении инструкции выполняется чтение с сервера результирующего набора полностью.<br/><br/> **Примечание**. После обновления драйвера JDBC с версии 1.2 по умолчанию будет использоваться режим буферизации adaptive. Если приложение никогда не задает свойство responseBuffering и требуется сохранить в приложении режим по умолчанию как в версии 1.2, необходимо задать свойству responseBufferring значение full в свойствах подключения или с помощью метода [setResponseBuffering](../../connect/jdbc/reference/setresponsebuffering-method-sqlserverstatement.md) объекта [SQLServerStatement](../../connect/jdbc/reference/sqlserverstatement-class.md). |
| selectMethod<br/><br/>String<br/>["прямой" &#124; "курсор"]<br/><br/>direct | Если этому свойству задано значение "cursor", то для каждого созданного в этом подключении запроса с курсорами **TYPE_FORWARD_ONLY** и **CONCUR_READ_ONLY** создается курсор базы данных. Это свойство обычно необходимо, только если приложение создает очень большие результирующие наборы, которые не могут полностью находиться в клиентской памяти. Если свойство имеет значение cursor, в клиентской памяти сохраняется ограниченное число строк результирующего набора. <br/><br/>Режим по умолчанию — все строки результирующего набора сохраняются в клиентской памяти. Такой вариант обеспечивает наибольшую производительность, когда приложение обрабатывает все строки. |
| Сендстрингпараметерс...<br/>асуникоде<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>true | *sendStringParametersAsUnicode*<br/><br/>Если свойство **sendStringParametersAsUnicode** имеет значение true, то строковые параметры отправляются на сервер в формате Юникода.<br/><br/> Если свойство **sendStringParametersAsUnicode** имеет значение false, то строковые параметры отправляются на сервер в формате, отличном от Юникода, например ASCII/MBCS.<br/><br/> Значение по умолчанию для свойства **sendStringParametersAsUnicode** — true.<br/><br/> **Примечание**. Проверка свойства **sendStringParametersAsUnicode** выполняется только при отправке значения параметра со следующими типами JDBC: **CHAR**, **VARCHAR** или **LONGVARCHAR**. Новые методы национальных символов в JDBC 4.0, например методы setNString, setNCharacterStream и setNClob классов [SQLServerPreparedStatement](../../connect/jdbc/reference/sqlserverpreparedstatement-class.md) и [SQLServerCallableStatement](../../connect/jdbc/reference/sqlservercallablestatement-class.md), всегда выполняют отправку на сервер своих значений параметров в формате Юникода, независимо от значения свойства.<br/><br/> Для оптимальной производительности при работе с типами данных JDBC **CHAR**, **VARCHAR** и **LONGVARCHAR** приложение должно установить свойство **sendStringParametersAsUnicode** в значение false и использовать методы, не поддерживающие национальные символы: setString, setCharacterStream и setClob классов [SQLServerPreparedStatement](../../connect/jdbc/reference/sqlserverpreparedstatement-class.md) и [SQLServerCallableStatement](../../connect/jdbc/reference/sqlservercallablestatement-class.md).<br/><br/> Когда приложение задает свойству **sendStringParametersAsUnicode** значение false и использует метод, не поддерживающий национальные символы, для доступа на стороне сервера к типам данных в формате Юникода (например, **nchar**, **nvarchar** и **ntext**) часть данных может быть потеряна, если сортировка базы данных не поддерживает символы в строковых параметрах, которые передаются методом, не поддерживающим национальные символы.<br/><br/> Обратите внимание, что приложение должно использовать национальные символы setNString, setNCharacterStream и setNClob для классов [SQLServerPreparedStatement](../../connect/jdbc/reference/sqlserverpreparedstatement-class.md) и [SQLServerCallableStatement](../../connect/jdbc/reference/sqlservercallablestatement-class.md) для **nchar**, **nvarchar**, и **лонгнварчар** типы данных JDBC. |
| sendTimeAsDatetime<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>true | Это свойство добавлено в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] JDBC Driver 3.0.<br/><br/> Задайте значение "true", чтобы отправить значения Java. SQL. Time на сервер в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] качестве значений **даты и времени** . <br/>Задайте значение "false", чтобы отправить значения Java. SQL. Time на сервер в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] качестве значений **времени** .<br/><br/> Значение этого свойства по умолчанию в настоящее время равно true и может быть изменено в следующем выпуске.<br/><br/> Дополнительные сведения о [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] настройке значений Java. SQL. time перед их отправкой на сервер см. в разделе [Настройка отправки значений Java. SQL. Time на сервер](../../connect/jdbc/configuring-how-java-sql-time-values-are-sent-to-the-server.md). |
| serverName,<br/>сервер<br/><br/>String<br/><br/>null | Компьютер, на котором работает [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)].<br/><br/> Можно также указать имя виртуальной сети в группе доступности [!INCLUDE[ssHADR](../../includes/sshadr_md.md)]. См. подробнее о [поддержке высокого уровня доступности и аварийного восстановления в драйвере JDBC](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md). |
| serverNameAsACE<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>false | Начиная с Microsoft JDBC Driver 6.0 для SQL Server, задайте значение true, чтобы указать, что драйвер должен преобразовать имя сервера в формате Юникода в ASCII-совместимую кодировку (Punycode) для данного подключения. Если этот параметр имеет значение false, драйвер подключается с использованием имени сервера, указанного пользователем.<br/><br/> Дополнительные сведения см. [в статье международные функции драйвера JDBC](../../connect/jdbc/international-features-of-the-jdbc-driver.md) . |
| Серверпрепаредстатемент...<br/>дискардсрешолд<br/><br/>Целочисленный<br/><br/>10 | *серверпрепаредстатементдискардсрешолд*<br/><br/>Начиная с драйвера JDBC 6,2 для SQL Server это свойство можно использовать для управления количеством невыполненных операций отмены подготовленных инструкций<code>sp_unprepare</code>(), которые могут быть недоступными для каждого соединения, прежде чем будет выполнен вызов очистки необработанных дескрипторов на сервере. . <br/><br/> Если для &lt;этого свойства задано значение = 1, то при закрытии подготовленных инструкций немедленно выполняются неподготовительные действия. Если задано значение &gt;1, эти вызовы объединяются в пакеты, чтобы избежать чрезмерных затрат на вызов sp_unprepare слишком часто. |
| serverSpn<br/><br/>String<br/><br/>null | Начиная с версии Microsoft JDBC Driver 4.2 для SQL Server это необязательное свойство можно использовать для указания имени субъекта-службы (SPN) соединения Java Kerberos.  Он используется совместно с **аусентикатионсчеме**.<br/><br/> Для указания имени субъекта-службы оно может быть в виде "MSSQLSvc/fqdn:port@REALM", где fqdn — это полное доменное имя, port — номер порта, а REALM — это область Kerberos SQL Server в верхнем регистре.<br/><br/> Примечание. Параметр @REALM является необязательным, если область по умолчанию клиента (как указано в конфигурации Kerberos) совпадает с областью Kerberos для SQL Server.<br/><br/> Дополнительные сведения об использовании **serverSpn** с Java Kerberos см. в статье [использование встроенной проверки подлинности Kerberos для подключения к SQL Server](../../connect/jdbc/using-kerberos-integrated-authentication-to-connect-to-sql-server.md). |
| Статементпулинг...<br/>CacheSize<br/><br/>INT<br/><br/>0 | *статементпулингкачесизе*<br/><br/>Начиная с драйвера JDBC 6,4 для SQL Server это свойство можно использовать для включения кэширования подготовленных обработчиков инструкций в драйвере. <br/><br/>Это свойство определяет размер кэша для объединения инструкций в пул. <br/><br/>Это свойство можно использовать только в сочетании со свойством соединения **disableStatementPooling** , для которого необходимо задать значение "false". При установке для **disableStatementPooling** значения "true" или **статементпулингкачесизе** в значение 0 отключается кэширование подготовленных обработчиков инструкций.|
| соккеттимеаут<br/><br/>INT<br/><br/>0 | Время ожидания в миллисекундах до истечения времени ожидания для чтения или принятия сокета. Значение по умолчанию — 0, что означает бесконечное время ожидания. |
| сслпротокол<br/><br/>String<br/><br/>TLS | Начиная с драйвера JDBC 6,4 для SQL Server это свойство можно использовать для указания протокола TLS, который будет учитываться при безопасном соединении. <br/>Возможные значения: **TLS**, **TLSv1**, **TLSv1.1** и **TLSv1.2**. <br/><br/>Дополнительные сведения см. в разделе [сслпротокол](https://github.com/Microsoft/mssql-jdbc/wiki/SSLProtocol). |
| Транспарентнетворк...<br/>ипресолутион<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>true | *transparentNetworkIPResolution*<br/><br/>Начиная с Microsoft JDBC Driver 6,0 для SQL Server это свойство обеспечивает более быстрое обнаружение и подключение к активному серверу (в настоящее время). Возможные значения: "true" и "false", где "true" является значением по умолчанию.<br/><br/> Перед установкой драйвера Microsoft JDBC Driver 6,0 для SQL Server приложению пришлось установить строку подключения с параметром multiSubnetFailover = true, чтобы указать, что он подключается к группе доступности AlwaysOn. Без установки ключевого слова подключения **MultiSubnetFailover** в значение "true" при подключении к группе доступности AlwaysOn может произойти время ожидания приложения. Начиная с Microsoft JDBC Driver 6,0 для SQL Server приложению не нужно задавать значение true для параметра multiSubnetFailover. <br/><br/>**Примечание.** Если transparentNetworkIPResolution = true, первая попыток соединения использует 500 мс в качестве времени ожидания. Все последующие попытки используют ту же логику времени ожидания, что и свойство multiSubnetFailover. |
| trustManagerClass<br/><br/>String<br/><br/>null | Полное имя класса пользовательской <code>javax.net.ssl.TrustManager</code> реализации. |
| trustManager...<br/>конструкторарг<br/><br/>String<br/><br/>null | *трустманажерконструкторарг*<br/><br/>Необязательный аргумент для передачи в конструктор TrustManager. Если указан параметр trustManagerClass и запрошено зашифрованное соединение, то используется настраиваемый TrustManager, а не система по умолчанию TrustManager на основе хранилища ключей System ВИРТУАЛЬНОЙ машины Java. |
| trustServerCertificate<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>false | Установите значение true, чтобы [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] не проверял SSL-сертификат [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)].<br/><br/> Если значение — true, SSL-сертификат [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] автоматически считается доверенным, когда для шифрования уровня связи используется SSL.<br/><br/> Если значение равно "false" [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] , то проверяет SSL-сертификат сервера. Если проверка сертификата сервера завершается ошибкой, в работе драйвера будет вызвана ошибка и подключение будет разорвано. Значение по умолчанию — «false». Для успешного установления SSL-подключения передаваемое в **serverName** значение должно в точности совпадать с общим именем (CN) или DNS-именем в альтернативном имени субъекта (SAN) сертификата сервера. См. [основные сведения о поддержке SSL](../../connect/jdbc/understanding-ssl-support.md).<br/><br/> **Примечание.** Это свойство используется в сочетании со свойствами **шифрования**/для**проверки** подлинности. Это свойство влияет только на проверку SSL-сертификата сервера только в том случае, если соединение использует шифрование SSL. |
| trustStore<br/><br/>String<br/><br/>null | Путь к файлу сертификата trustStore (включая имя файла). Файл trustStore содержит список сертификатов, которым доверяет клиент.<br/><br/> Когда свойство не задано или имеет значение null, драйвер будет использовать правила поиска фабрики диспетчеров доверия, чтобы определить, какой из хранящихся сертификатов необходимо использовать.<br/><br/> **Используемая по умолчанию фабрика SunX509 TrustManagerFactory пытается найти доверенный ресурс, производя поиск в следующем порядке:**<br/><br/> Файл, указанный системным свойством javax.net.ssl.trustStore виртуальной машины Java (JVM).<br/><br/> Файл "&lt;домашний_каталог-java&gt;/lib/security/jssecacerts".<br/><br/> Файл "&lt;домашний_каталог-java&gt;/lib/security/cacerts".<br/><br/> <br/><br/> Дополнительные сведения см. в документации по интерфейсу SUNX509 TrustManager на веб-сайте компании Sun Microsystems.<br/><br/> **Примечание.** Это свойство влияет только на сертификат trustStore Lookup, только если соединение использует SSL-шифрование, а свойство **trustServerCertificate** имеет значение "false". |
| trustStorePassword<br/><br/>String<br/><br/>null | Пароль, используемый для проверки целостности данных trustStore.<br/><br/> Если задано свойство trustStore, а свойство trustStorePassword не задано, проверка целостности trustStore не выполняется.<br/><br/> Если значение не задано как для свойства trustStore, так и для свойства trustStorePassword, драйвер будет использовать системные свойства виртуальной машины Java javax.net.ssl.trustStore и javax.net.ssl.trustStorePassword. Если не задано значение системного свойства "javax.net.ssl.trustStorePassword", проверка целостности trustStore не выполняется.<br/><br/> Если свойство trustStore не задано, а свойство trustStorePassword задано, драйвер JDBC будет использовать файл, заданный свойством javax.net.ssl.trustStore, в качестве доверенного хранилища, а целостность доверенного хранилища будет проверяться с использованием указанного trustStorePassword. Это может потребоваться в случае, если клиентское приложение не желает сохранять пароль с системном свойстве JVM.<br/><br/> **Примечание.**  Свойство trustStorePassword влияет только на подстановку сертификата trustStore, только если соединение использует SSL-соединение, а свойство **trustServerCertificate** имеет значение false. |
| трустсторетипе<br/><br/>String<br/><br/>JKS- | Задайте это свойство, чтобы указать тип хранилища доверия, который будет использоваться для режима FIPS. <br/><br/>Возможные значения: **PKCS12** или Type, определенные поставщиком FIPS. |
| Усебулккопифор...<br/>BatchInsert<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>false | _усебулккопифорбатчинсерт_<br/><br/> Начиная с Microsoft JDBC Driver 7,0 для SQL Server это свойство соединения можно включить, чтобы использовать API-интерфейс копирования при выполнении операций пакетной вставки, используя `java.sql.PreparedStatement` для повышения производительности. <br/><br/>Эта функция работает только в том случае, если целевой сервер относится к типу **хранилища данных Azure**. По умолчанию он отключен, чтобы включить эту функцию, установите для этого свойства значение true. <br/></br> **Важное замечание.** Эта функция поддерживает только полностью параметризованные запросы INSERT. Если запросы INSERT объединяются другими запросами SQL или содержат данные в значениях, выполнение будет возвращаться к базовой операции пакетной вставки. <br/><br/> Дополнительные сведения об использовании этого свойства см. в разделе [Использование API-интерфейса для операции пакетной вставки](use-bulk-copy-api-batch-insert-operation.md) .|
| useFmtOnly<br /><br />boolean<br />["true" &#124; "false"]<br /><br />false | Драйвер Microsoft JDBC Driver для SQL Server starting v 7.4 предоставляет альтернативный способ запроса метаданных параметров с сервера путем указания свойства соединения **усефмтонли** . Задайте для этого свойства значение "true", чтобы указать, что драйвер `SET FMTONLY` должен использовать логику при запросе метаданных параметров. Эта функция отключена по умолчанию, поэтому не рекомендуется использовать это свойство, так как `SET FMTONLY` помечено для устаревания. **усефмтонли** предоставляется для использования только в качестве обходного решения для известных проблем и ограничений в [`sp_describe_undeclared_parameters`](https://docs.microsoft.com/sql/relational-databases/system-stored-procedures/sp-describe-undeclared-parameters-transact-sql?view=sql-server-2017).<br/><br/> Сейчас эта функция поддерживает только отдельные `SELECT/INSERT/UPDATE/DELETE` запросы. Попытка использовать эту функцию с неподдерживаемыми или несколькими запросами приведет к тому, что драйвер попытается выполнить синтаксический анализ запросов, но, скорее всего, приведет к исключению.<br/><br/> Дополнительные сведения см. в разделе [Получение ParameterMetaData через усефмтонли](../../connect/jdbc/using-usefmtonly.md). |
| userName,<br/>пользователь<br/><br/>String<br/>[&lt;=128 char]<br/><br/>null | Пользователь базы данных, в случае соединения с пользователем и паролем SQL.<br/><br/>Для подключения Kerberos с именем участника и паролем этому свойству присваивается имя участника Kerberos. |
| workstationID<br/><br/>String<br/>[&lt;=128 char]<br/><br/>&lt;пустая строка&gt; | Идентификатор рабочей станции. Используется для идентификации определенной рабочей станции в различных средствах профилирования и ведения журнала [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]. <br/><br/>Если не задано, используется &lt;пустая строка&gt;. |
| xopenStates<br/><br/>boolean<br/>["true" &#124; "false"]<br/><br/>false | Установите значение true, чтобы указать, что драйвер возвращает в исключениях коды состояний, совместимые с XOPEN. <br/><br/>Значение по умолчанию — возвращать коды состояний SQL 99. |
| &nbsp; | &nbsp; |

> [!NOTE]  
> Драйвер [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] принимает значения сервера по умолчанию для свойств подключения, за исключением ANSI_DEFAULTS и IMPLICIT_TRANSACTIONS. Драйвер [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] автоматически устанавливает свойство ANSI_DEFAULTS в значение ON, а свойство IMPLICIT_TRANSACTIONS — в значение OFF.

> [!Important]
> Если для проверки подлинности задано значение ActiveDirectoryPassword, необходимо добавить следующую библиотеку в подкаталог классов: [Azure-ActiveDirectory-Library-for-Java](https://github.com/AzureAD/azure-activedirectory-library-for-java). Его можно найти в [репозитории Maven](https://mvnrepository.com/artifact/com.microsoft.azure/adal4j). Самый простой способ скачать библиотеку и ее зависимости — использовать Maven: 
> 1. Сначала установите Maven в системе. 
> 2. Перейдите на [страницу GitHub](https://github.com/Microsoft/mssql-jdbc) драйвера.
> 3. Скачивание файла pom. XML
> 4. Выполните следующую команду Maven, чтобы скачать библиотеку и ее зависимости:`mvn dependency:copy-dependencies`

## <a name="see-also"></a>См. также:

[Соединение с SQL Server с помощью драйвера JDBC](../../connect/jdbc/connecting-to-sql-server-with-the-jdbc-driver.md)  
[Режим FIPS](../../connect/jdbc/fips-mode.md)
