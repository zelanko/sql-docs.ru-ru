---
title: Параметры (преобразование) (OracleToSQL) проекта | Документация Майкрософт
ms.prod: sql
ms.custom: ''
ms.date: 01/19/2017
ms.reviewer: ''
ms.technology: ssma
ms.topic: conceptual
ms.assetid: a98a5e07-eb5e-47b9-a6f2-e2cb3a18309c
author: Shamikg
ms.author: Shamikg
manager: v-thobro
ms.openlocfilehash: bff48432749d6886f58c985adc9cb779303edb17
ms.sourcegitcommit: 1ab115a906117966c07d89cc2becb1bf690e8c78
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/27/2018
ms.locfileid: "52410311"
---
# <a name="project-settings-conversion-oracletosql"></a>Параметры проекта (преобразование) (OracleToSQL)
На странице преобразования **параметры проекта** диалоговое окно содержит настройки, установленные как SSMA преобразует синтаксис Oracle [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] синтаксис.  
  
Область преобразования доступна в **параметры проекта** и **параметры проекта по умолчанию** диалоговые окна:  
  
-   Для задания параметров для всех проектов SSMA на **средства** меню **параметры проекта по умолчанию**, выберите тип проекта миграции, для которого требуются параметры для просмотра и изменения из  **Целевой версии миграции** раскрывающийся список, а затем щелкните **Общие** в нижней части левой панели, а затем нажмите кнопку **преобразования**.  
  
-   Для задания параметров для текущего проекта на **средства** меню **параметры проекта**, нажмите кнопку **Общие** в нижней части левой панели и нажмите кнопку **Преобразование**.  
  
## <a name="conversion-messages"></a>Преобразование сообщений  
  
|||  
|-|-|  
|Термин|Определение|  
|**Создают сообщения о проблемах, которые применены**|Разрешение SSMA создает информационных сообщений во время преобразования, отображает их в области вывода и добавляет их в преобразованном коде.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Нет<br /><br />**Полный режим:** Нет|  
  
## <a name="miscellaneous-options"></a>Различные параметры  
  
|||  
|-|-|  
|Термин|Определение|  
|**Выражения приведения ROWNUM как целые числа**|Когда SSMA преобразует ROWNUM выражения, он преобразует их в предложение TOP, а затем с помощью выражения. В следующем примере показано ROWNUM в инструкцию Oracle DELETE:<br /><br />`DELETE FROM Table1`<br /><br />`WHERE ROWNUM < expression and Field1 >= 2`<br /><br />В следующем примере показан итоговый [!INCLUDE[tsql](../../includes/tsql-md.md)]:<br /><br />`DELETE TOP (expression-1)`<br /><br />`FROM Table1`<br /><br />`WHERE Field1>=2`<br /><br />ВЕРХНЕЙ требует, что предложения TOP выражение принимает значение является целым числом. Если целое число является отрицательным, инструкция вызовет ошибку.<br /><br />При выборе **Да**, SSMA приведение выражения к целому числу.<br /><br />При выборе **нет**, SSMA пометит все выражения, отличный от integer в преобразованный код как ошибка.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/Full:** Нет<br /><br />**Режим оптимистичного:** Да|  
|**Сопоставление схемы по умолчанию**|Этот параметр указывает сопоставление схем Oracle со схемами SQL Server. В этом параметре, доступны два варианта:<br /><br />**Схемы в базу данных:** В этой схеме Oracle режим «sch1» будет сопоставляться по умолчанию для схемы «dbo» SQL Server в базе данных SQL Server «sch1».<br /><br />**Схемы для схемы:** в этом режиме Oracle схемы «sch1» будет сопоставляться по умолчанию для схемы «sch1» SQL Server в базе данных SQL Server по умолчанию, в диалоговом окне соединения.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Схемы в базу данных|  
|**Способы преобразования инструкции MERGE**|При выборе **использование INSERT, UPDATE, инструкция DELETE**, SSMA преобразуется СЛИЯНИЯ инструкции INSERT, UPDATE, DELETE инструкции.<br /><br />При выборе **инструкции с помощью СЛИЯНИЯ**, SSMA преобразует инструкции СЛИЯНИЯ в инструкции MERGE в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)].<br /><br />**Примечание.** Этот параметр проекта параметр доступен только в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] 2008 [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] 2012, [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] 2014.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** С помощью инструкции MERGE|  
|**Преобразования вызовов для подпрограммы, использующие аргументы по умолчанию**|[!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] функции не поддерживают Пропуск аргументов в вызове функции. Кроме того [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] функции и процедуры не поддерживают выражения, как значения параметров по умолчанию.<br /><br />При выборе **Да** и вызов функции опускает параметров, SSMA вставит ключевое слово **по умолчанию** в функцию и вызовите в правильном положении. Затем он помечает вызов с предупреждением.<br /><br />При выборе **нет**, SSMA пометит вызовы функций, как ошибки.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразовать функции COUNT и COUNT_BIG**|Если ваши функции COUNT, скорее всего для возврата значений, размер которых превышает 2 147 483 647, который составляет 2<sup>31</sup>-1, следует преобразовать функции COUNT_BIG.<br /><br />При выборе **Да**, SSMA преобразует все случаи использования число COUNT_BIG.<br /><br />При выборе **нет**, функции будет оставаться COUNT. [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] Возвращает ошибку, если функция возвращает значение, размер которых превышает 2<sup>31</sup>-1.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/Full:** Да<br /><br />**Режим оптимистичного:** Нет|  
|**Преобразовать FORALL оператор WHILE инструкции**|Определяет, как интерпретировать SSMA FORALL циклы в коллекции элементов PL/SQL.<br /><br />При выборе **Да**, SSMA создает цикл WHILE, где элементы коллекции, полученные по одному.<br /><br />При выборе **нет**, SSMA создает набор строк из коллекции, используя метод узлов () и использует его как одна таблица. Это более эффективно, но становится менее удобочитаемым выходной код.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Нет<br /><br />**Полный режим:** Да|  
|**Внешние ключи Convert со ссылочным действием SET NULL для столбца, который является NOT NULL.**|Oracle допускает создания ограничения внешнего ключа, где действием SET NULL возможно невозможно, так как значения NULL не разрешены в указываемом столбце. [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] не поддерживает такой конфигурации внешнего ключа.<br /><br />При выборе **Да**, SSMA создаст ссылочные действия, как и в Oracle, но необходимо будет внести изменения вручную перед загрузкой, чтобы ограничение [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]. Например можно выбрать NO ACTION, а не значение NULL.<br /><br />При выборе **нет**, ограничение отмечается как ошибка.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Нет|  
|**Преобразовать вызовы функций, чтобы вызовы процедур**|Некоторые функции Oracle определяются как автономные транзакции или содержать инструкции, которые не будет действителен в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]. В этих случаях SSMA создает процедуры и функции, которая является оболочкой для процедуры. Преобразованный функция вызывается процедура реализации.<br /><br />SSMA можно преобразовать вызовов функции-оболочки в вызова процедуры. Это создает более удобочитаемый код и может повысить производительность. Тем не менее контекст не всегда разрешено; Например невозможно заменить вызов функции в списке SELECT при вызове процедуры. SSMA имеет несколько параметров для распространенных случаев:<br /><br />При выборе **всегда**, SSMA пытается преобразовать вызовы функций программы-оболочки в вызовы процедур. Если текущий контекст не позволяет это преобразование, создается сообщение об ошибке. Таким образом, не вызовы функций, остаются в созданном коде.<br /><br />При выборе **по возможности**, SSMA делает перемещения для вызовов процедур, только в том случае, если функция выходных параметров. Перемещение не поддерживается, выходной атрибут параметра будет удалено. Во всех остальных случаях SSMA оставляет вызовы функций.<br /><br />При выборе **никогда**, SSMA оставит всех вызовов функций, как вызовы функций. Иногда этот выбор может быть неприемлемо из-за повышения производительности.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Если это возможно|  
|**Преобразовать инструкции БЛОКИРОВКИ таблицы**|SSMA можно преобразовать много инструкций БЛОКИРОВКИ таблицы в табличных подсказок. SSMA не удается преобразовать операторов БЛОКИРОВКИ таблицы, которая содержит СЕКЦИЮ, ПОДРАЗДЕЛ, @dblinkи предложения NOWAIT и пометит такие инструкции, с сообщениями об ошибках преобразования.<br /><br />При выборе **Да**, SSMA преобразует поддерживаемых инструкций БЛОКИРОВКИ таблицы в табличных подсказок.<br /><br />При выборе **нет**, SSMA пометит все инструкции БЛОКИРОВКИ таблицы с сообщениями об ошибках преобразования.<br /><br />В следующей таблице показаны как SSMA преобразует режимов блокировки Oracle:<br /><br />**Режим блокировки Oracle**<br /><br />СТРОКИ РЕСУРСА<br /><br />ЭКСКЛЮЗИВНЫЕ СТРОКИ<br /><br />ОБНОВЛЕНИЕ ОБЩЕГО РЕСУРСА = СТРОКИ РЕСУРСА<br /><br />ПРЕДОСТАВИТЬ ОБЩИЙ ДОСТУП<br /><br />ПРЕДОСТАВИТЬ ОБЩИЙ ДОСТУП<br /><br />ЭКСКЛЮЗИВНЫЕ<br /><br />**SQL Server табличное Указание**<br /><br />ROWLOCK, HOLDLOCK<br /><br />HOLDLOCK ROWLOCK, XLOCK,<br /><br />ROWLOCK, HOLDLOCK<br /><br />TABLOCK, HOLDLOCK<br /><br />HOLDLOCK TABLOCK, XLOCK,<br /><br />TABLOCKX, HOLDLOCK<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразовать инструкции OPEN FOR для параметров REF CURSOR OUT**|В Oracle инструкция OPEN FOR можно использовать для возврата результирующего набора для ВЫХОДНОЙ параметр типа REF CURSOR подпрограмму. В [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)], хранимые процедуры напрямую возвращать результаты инструкции SELECT.<br /><br />SSMA можно преобразовать многие инструкции OPEN FOR в инструкции SELECT.<br /><br />При выборе **Да**, SSMA преобразует инструкции OPEN FOR в инструкции SELECT, который возвращает результирующий набор клиенту.<br /><br />При выборе **нет**, SSMA сформирует сообщение об ошибке, в преобразованный код и в области вывода.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразовать запись как список переменных разделяет**|SSMA можно преобразовать Oracle записей в разделяет переменные, а также в переменные XML с определенной структурой.<br /><br />При выборе **Да**, SSMA преобразует записи в список переменных разделяет, когда это возможно.<br /><br />При выборе **нет**, SSMA преобразует записи в переменных XML с определенной структурой.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразовать SUBSTR вызовы функций в вызовы функций SUBSTRING**|SSMA можно преобразовать Oracle SUBSTR вызовы функций в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] **подстроки** вызовы функций, в зависимости от количества параметров. Если SSMA не удается преобразовать вызов функции SUBSTR или количество параметров не поддерживается, SSMA преобразует в вызов функции пользовательских SSMA SUBSTR вызова функции.<br /><br />При выборе **Да**, преобразует вызовы функций SUBSTR с использованием трех параметров в SSMA [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] **подстроки**. Другие функции SUBSTR преобразуется в вызов пользовательской функции SSMA.<br /><br />При выборе **нет**, SSMA преобразует в вызов функции пользовательских SSMA SUBSTR вызова функции.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Да<br /><br />**Полный режим:** Нет|  
|**Преобразовать подтипы**|SSMA можно преобразовать подтипы PL/SQL, двумя способами:<br /><br />При выборе **Да**, создаст SSMA [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] определяемых пользователем из подтипом типа и использовать его для каждой переменной этого подтипа.<br /><br />При выборе **нет**, SSMA будет заменить все объявления источника подтипа с базовым типом и обычным образом преобразовать результат. В этом случае дополнительные типы не создаются в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Нет|  
|**Преобразовать синонимы**|Синонимы для следующих объектов Oracle могут быть перенесены в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]:<br /><br />Таблицы и таблицы, объект<br /><br />Представления и представления объектов<br /><br />Хранимые процедуры и функции<br /><br />Материализованные представления<br /><br />**Синонимы для следующих** Oracle объекты могут быть заменены прямые ссылки на объекты:<br /><br />Последовательности;<br /><br />Пакеты<br /><br />Объекты схемы классов в Java<br /><br />Пользовательские объектные типы<br /><br />Другие синонимы станет невозможна. SSMA создает сообщения об ошибках для синонима и все ссылки, которые используют синоним.<br /><br />При выборе **Да**, создаст SSMA [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] синонимов и прямые объектные ссылки в соответствии с в списках выше.<br /><br />При выборе **нет**, SSMA создаст прямые объектные ссылки для все синонимы, перечисленные здесь.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразовать TO_CHAR (даты, формат)**|SSMA можно преобразовать Oracle TO_CHAR(date, format) в процедуры из базы данных sysdb.<br /><br />При выборе **TO_CHAR_DATE с помощью функции**, SSMA преобразует TO_CHAR (даты, формат) в TO_CHAR_DATE функцию с помощью английского языка для преобразования.<br /><br />При выборе **TO_CHAR_DATE_LS с помощью функции (NLS осторожностью)**, SSMA преобразует TO_CHAR (даты, формат) в TO_CHAR_DATE_LS функцию с помощью языка сеанса для преобразования<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** С помощью функции TO_CHAR_DATE<br /><br />**Полный режим:** С помощью функции TO_CHAR_DATE_LS (NLS осторожностью)|  
|**Преобразовать инструкции обработки транзакций**|SSMA можно преобразовать инструкции обработки транзакций Oracle.<br /><br />При выборе **Да**, SSMA преобразует инструкции обработки транзакций Oracle для [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] инструкций.<br /><br />При выборе **нет**, SSMA помечает транзакцию, обработка инструкций как ошибки преобразования.<br /><br />**Примечание.**  Oracle открывает транзакции неявно. Чтобы эмулировать такое поведение на [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)], необходимо добавить инструкции BEGIN TRANSACTION вручную место для запуска транзакции. Кроме того можно выполнить команду SET IMPLICIT_TRANSACTIONS ON в начале сеанса. SSMA автоматически добавляет SET IMPLICIT_TRANSACTIONS ON, при преобразовании подпрограммы с автономной транзакциями.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Эмулировать поведение null Oracle в предложениях ORDER BY**|Значения NULL упорядочиваются по-разному в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] и Oracle:<br /><br />В [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)], или значение NULL — это наименьшее значения в упорядоченном списке. Из списка по возрастанию значений NULL будут отображаться первыми.<br /><br />В Oracle значения NULL являются самыми высокими значениями в упорядоченном списке. По умолчанию значения NULL отображаются последними в возрастающем порядке.<br /><br />Oracle имеет значения NULL и пустые значения ФАМИЛИИ предложений, который можно изменить, как Oracle упорядочивает значения NULL.<br /><br />SSMA можно эмулировать поведение Oracle ORDER BY, проверка значений NULL. Он сначала упорядочивает значениями NULL в указанном порядке, а затем заказы на другие значения.<br /><br />При выборе **Да**, SSMA преобразует инструкции Oracle способом, который эмулирует поведение Oracle ORDER BY.<br /><br />При выборе **нет**, SSMA будет игнорировать правила Oracle и создать сообщение об ошибке при обнаружении первого значения NULL и пустые значения ПОСЛЕДНЕГО предложения.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Нет<br /><br />**Полный режим:** Да|  
|**Эмуляция исключения число строк в SELECT**|Если инструкция SELECT с предложением INTO не возвращает строк, Oracle создает исключение NO_DATA_FOUND. Если инструкция возвращает две или несколько строк, TOO_MANY_ROWS исключение. Инструкцию, преобразованное в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] не вызывает любое исключение, если число строк не совпадает с одним.<br /><br />При выборе **Да**, SSMA добавляет вызов процедуры db_error_exact_one_row_check sysdb после каждой инструкции SELECT. Эта процедура имитирует NO_DATA_FOUND и TOO_MANY_ROWS исключения. Это значение по умолчанию и позволяет воспроизводить поведение Oracle, как можно ближе. Следует всегда выбирать **Да** Если исходный код содержит обработчики исключений, которые обрабатывают эти ошибки. Обратите внимание, что в случае запроса SELECT внутри определяемой пользователем функции этого модуля будут преобразовываться в хранимую процедуру, так как выполнение хранимых процедур и создание исключений не совместим с [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] контексте функции.<br /><br />При выборе **нет**, исключения не создаются. Что может быть полезно, когда SSMA преобразует определяемой пользователем функции, и вы хотите, чтобы он оставался в функцию в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Вызывают ошибку для DBMS_SQL. СИНТАКСИЧЕСКИЙ АНАЛИЗ**|При выборе **ошибка**, SSMA вызывают ошибку в преобразование DBMS_SQL. ВЫПОЛНИТЕ СИНТАКСИЧЕСКИЙ АНАЛИЗ.<br /><br />При выборе **предупреждение**, SSMA предупреждение в преобразование DBMS_SQL. ВЫПОЛНИТЕ СИНТАКСИЧЕСКИЙ АНАЛИЗ.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Ошибка|  
|**Сформировать столбец ROWID**|Когда SSMA создает таблицы в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)], его можно создать столбец ROWID. При миграции данных, каждая строка получает новое значение UNIQUEIDENTIFIER, созданные с помощью функции newid().<br /><br />При выборе **Да**, ROWID столбца создается со всеми таблицами и [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] создает идентификаторы GUID, как во время вставки значений. Всегда выбирайте **Да** Если вы планируете использовать тест-инженер SSMA.<br /><br />При выборе **нет**, ROWID столбцы не добавляются в таблицы.<br /><br />**Добавить столбец ROWID для таблиц с триггерами** добавьте ROWID для таблиц, содержащих триггеры.<br /><br />**Примечание.** Значение по умолчанию в случае SQL Server 2005, SQL Server 2008 и SQL Server 2012 и 2014 — **ROWID, добавьте столбец для таблиц с триггерами**.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Добавить столбец ROWID для таблиц с триггерами<br /><br />**Полный режим:** Да|  
|**Создать уникальный индекс по столбцу ROWID**|Указывает, создает ли SSMA столбца уникальный индекс по столбцу ROWID создан. Если параметр имеет значение «Да», создается уникальный индекс, и если он имеет значение «Нет», не создается уникальный индекс по столбцу ROWID.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Локальные модули преобразования**|Определяет тип выполняемого преобразования oracle вложенные подпрограмме (объявлено в автономных хранимой процедуре или функции).<br /><br />При выборе **встроенного**, вызовы вложенных подпрограмме заменяется его основной части.<br /><br />При выборе **хранимых процедур**вложенных подпрограмме будут преобразовываться в хранимую процедуру SQL Server и его вызовы будут заменены на этот вызов процедуры.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** встроенный|  
|**Использовать ISNULL в объединение строк**|Oracle и [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] возвращать разные результаты при конкатенации строк содержать значения NULL. Oracle обрабатывает значения NULL как набор пустой символ. [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] Возвращает значение NULL.<br /><br />При выборе **Да**, SSMA заменяет символ объединения Oracle (&#124;&#124;) с [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] символ объединения (+). SSMA также проверяет выражения с обеих сторон сцепление значений NULL.<br /><br />При выборе **нет**, SSMA заменяет символы объединения, но не проверяет наличие значений NULL.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**В вызовах функций REPLACE следует использовать ISNULL**|Инструкция ISNULL используется в вызовах функций REPLACE для эмуляции режима Oracle. Для этого параметра присутствуют следующие параметры:<br /><br />YES<br /><br />NO<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Нет<br /><br />**Полный режим:** Да|  
|**Использовать ISNULL в вызовах функций CONCAT**|Инструкция ISNULL используется в вызовах функции CONCAT для эмуляции режима Oracle. Для этого параметра присутствуют следующие параметры:<br /><br />YES<br /><br />NO<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Нет<br /><br />**Полный режим:** Да|  
|**Используйте функцию собственного convert, если это возможно**|При выборе **Да**, SSMA преобразует TO_CHAR (даты, формат) в функцию собственного convert, если это возможно.<br /><br />При выборе **нет**, SSMA преобразует TO_CHAR (даты, формат) в TO_CHAR_DATE или TO_CHAR_DATE_LS (он определен с параметрами «Преобразовать TO_CHAR(date, format)»).<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**По умолчанию/оптимистического режима:** Да<br /><br />**Полный режим:** Нет|  
|**Используйте инструкцию SELECT... FOR XML, при преобразовании ВЫБЕРИТЕ... INTO для переменной записи**|Указывает, следует ли создавать при выборе в переменной записи задать результат XML.<br /><br />При выборе **Да**, инструкция SELECT возвращает XML.<br /><br />При выборе **нет**, инструкция SELECT возвращает результирующий набор.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Нет|  
  
## <a name="returning-clause-conversion"></a>ВОЗВРАЩАЕТ предложение преобразования  
  
|||  
|-|-|  
|Термин|Определение|  
|**Преобразование предложением RETURNING в инструкции DELETE в выходных данных**|Oracle предоставляет предложением RETURNING, чтобы немедленно получить удаленные значения. [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] предоставляет функциональные возможности с предложением OUTPUT.<br /><br />При выборе **Да**, SSMA превратит RETURNING предложения в инструкции DELETE предложения OUTPUT. Так как триггеры для таблицы можно изменить значения, возвращаемое значение может отличаться в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] чем в Oracle.<br /><br />При выборе **нет**, SSMA создаст инструкцию SELECT, прежде чем инструкции DELETE для получения возвращаемых значений.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразование предложением RETURNING в инструкции INSERT в выходных данных**|Oracle предоставляет предложением RETURNING, чтобы немедленно получить вставленные значения. [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] предоставляет функциональные возможности с предложением OUTPUT.<br /><br />При выборе **Да**, SSMA преобразует предложением RETURNING в инструкции INSERT в выходных данных. Так как триггеры для таблицы можно изменить значения, возвращаемое значение может отличаться в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] чем в Oracle.<br /><br />При выборе **нет**, SSMA эмулирует функциональные возможности Oracle, вставка, а затем выбрав значения из ссылочной таблицы.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
|**Преобразование RETURNING предложение в инструкции UPDATE в выходных данных**|Oracle предоставляет предложением RETURNING, чтобы немедленно получить обновленные значения. [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] предоставляет функциональные возможности с предложением OUTPUT.<br /><br />При выборе **Да**, SSMA преобразует RETURNING предложения в инструкции UPDATE в выходных данных предложения. Так как триггеры для таблицы можно изменить значения, возвращаемое значение может отличаться в [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] чем в Oracle.<br /><br />При выборе **нет**, SSMA создаст инструкций SELECT после инструкций UPDATE для получения возвращаемых значений.<br /><br />Если выбран режим преобразования в **режим** », вкладка «SSMA применяется следующий параметр:<br /><br />**Режим по умолчанию/оптимистичный/Full:** Да|  
  
## <a name="sequence-conversion"></a>Преобразование последовательности  
  
|||  
|-|-|  
|Термин|Определение|  
|**Преобразовать генератор последовательностей**|В Oracle можно использовать для создания уникальных идентификаторов последовательности.<br /><br />SSMA можно преобразовать последовательностей ниже.<br /><br />С помощью генератора последовательности SQL Server (этот параметр доступен только при преобразовании в SQL Server 2012 и SQL Server 2014).<br /><br />Использование генератора последовательности SSMA.<br /><br />С помощью столбца удостоверения.<br /><br />Параметр по умолчанию при преобразовании в SQL Server 2012 или SQL Server 2014 является использование генератора последовательности, то SQL Server. Тем не менее SQL Server 2012 и SQL Server 2014 не поддерживает получение текущее значение последовательности (например, метода currval последовательности Oracle). Посетить сайт блога группы SSMA руководство по миграции метод currval последовательности Oracle.<br /><br />SSMA также предоставляет возможность преобразования последовательности Oracle к эмулятору SSMA последовательности. Это является параметром по умолчанию при преобразовании в SQL Server до версии 2012<br /><br />Наконец можно также преобразовать последовательность, назначенный столбцу в таблице, чтобы значения идентификаторов SQL Server. Необходимо указать сопоставление между последовательностями для столбца идентификаторов в Oracle **таблицы** вкладку|  
|**Преобразовать CURRVAL за пределами триггеров**|Отображается только в том случае, если преобразование генератора последовательности присвоено **с помощью столбца удостоверения**. Поскольку последовательности Oracle являются объектами, отдельно от таблиц, множество таблиц, использующих последовательностей использовать триггер создается и вставляется новое значение последовательности. SSMA эти инструкции в комментарии или помечает их как ошибки, когда комментирования out создают ошибки.<br /><br />При выборе **Да**, SSMA пометит все ссылки за пределами триггеры на преобразованное виртуализация CURRVAL с предупреждением.<br /><br />При выборе **нет**, SSMA пометит все ссылки за пределами триггеры на преобразованное виртуализация CURRVAL с ошибкой.|  
  
## <a name="see-also"></a>См. также  
[Справочник по пользовательскому интерфейсу &#40;OracleToSQL&#41;](../../ssma/oracle/user-interface-reference-oracletosql.md)  
  
